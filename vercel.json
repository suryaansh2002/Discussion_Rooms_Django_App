{
    "builds": [{
        "src": "learning/wsgi.py",
        "use": "@ardnt/python",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "learning/wsgi.py"
        }
    ]
}
